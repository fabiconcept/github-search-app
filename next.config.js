/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["https://github-lobby.sirv.com/", "github-lobby.sirv.com", "avatars.githubusercontent.com", "https://avatars.githubusercontent.com"]
    }
}

module.exports = nextConfig
